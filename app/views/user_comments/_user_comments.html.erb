<% user_comments.each do |user_comment| %>
  <tr>
    <td>
      <div>
        <p>
          Comment from
          <%= "#{user_comment.ip} at #{user_comment.created_at}" %>
        </p>
        <p>
          <%= raw user_comment.description %>
        </p>
      </td>

    </tr>
  <% end %>
