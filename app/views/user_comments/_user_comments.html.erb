<% user_comments.each do |user_comment| %>
  <tr>
    <td><%= raw user_comment.description %></td>
    <td><%= user_comment.ip %></td>
    <td><%= user_comment.created_at %></td>
    <td><%= link_to 'Destroy', user_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
